import { HighligntDirective } from './highlignt.directive';

describe('HighligntDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligntDirective();
    expect(directive).toBeTruthy();
  });
});
